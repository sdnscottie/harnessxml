module gitlab.com/visml/harnessxml/sdk/go

go 1.21

// No requires, deliberately. A specification that promises its released
// versions stay reachable forever should not need a dependency tree to read
// them.
